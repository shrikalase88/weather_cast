import 'package:flutter/material.dart';

class AppColors {
  static const Color base = Color(0xFF0A1929); // Deep navy
  static const Color surface = Color(0xFF112240); // Slightly lighter for cards
  static const Color accent = Color(0xFF00D1B2); // Cyan
  static const Color accentGlow = Color(0xFF64FFDA); // Glow
  
  static const Color textPrimary = Color(0xFFE6F1FF);
  static const Color textSecondary = Color(0xFFA8B2D1);
  
  static const Color shadowLight = Color(0xFF1B304D);
  static const Color shadowDark = Color(0xFF050C16);
}
